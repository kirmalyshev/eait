# eait — environment (copy to .env; .env is gitignored). No real secrets in this file.
# --- required ---
TELEGRAM_BOT_TOKEN=            # @eait_bot token (moved out of Pulse at go-live)
OPENROUTER_API_KEY=           # OpenRouter key — LLM spend runs on this

# --- LLM (defaults shown) ---
LLM_PROVIDER=openrouter
LLM_MODEL=qwen/qwen3-vl-235b-a22b-instruct
LLM_TIMEOUT_MS=60000

# --- storage / runtime (defaults shown) ---
DB_PATH=./data/eait.sqlite
PHOTO_DIR=./photos            # transient only — images are downloaded, analyzed, deleted
TZ=Europe/Berlin              # daily-total date boundary is computed in this zone, not UTC
PER_USER_DAILY_PHOTO_CAP=50

# --- admin ---
ADMIN_USER_ID=                # Telegram user id allowed to run /stats (optional)
