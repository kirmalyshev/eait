name: docker

# Builds and publishes the bot image to GHCR on every version tag, so self-hosters run
# `docker compose up` against ghcr.io/kirmalyshev/eait instead of cloning the repo and
# building locally (docker-compose.selfhost.yml consumes this image).
#
# Release flow: bump package.json version → git tag vX.Y.Z → git push --tags.

on:
  push:
    tags: ["v*"]
  workflow_dispatch: # manual rebuild of `latest` from the current default branch

permissions:
  contents: read
  packages: write

jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      # Gate the publish on a typecheck, in THIS job rather than a dependent one — a second
      # runner's startup dwarfs the ~1s check. The Dockerfile installs production deps only and
      # bun runs TypeScript without checking it, so nothing in the image build would notice a type
      # error: a tagged release could ship one straight to every self-hoster's `docker compose pull`.
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run typecheck
      # arm64 matters here: Raspberry Pi self-hosters and Apple-silicon docker both pull it.
      - uses: docker/setup-qemu-action@v4
      - uses: docker/setup-buildx-action@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/metadata-action@v6
        id: meta
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest
      - uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
