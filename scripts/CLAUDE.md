# CLAUDE.md — scripts/

Thin pointer. Dev/ops helpers only, never imported by the runtime. See `scripts/AGENTS.md`. Scripts that make real/billed calls (e.g. `smoke-openrouter.ts`) are manual and must not be wired into tests or the bot.
