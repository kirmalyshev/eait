{
  "name": "@eait/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build ./main.ts --outdir ./dist --target browser --minify",
    "start": "bun run server/index.ts",
    "typecheck": "tsc --noEmit && tsc --noEmit -p server && tsc --noEmit -p test"
  },
  "dependencies": {
    "@eait/shared": "workspace:*"
  }
}
