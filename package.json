{
  "name": "eait",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.4.0",
  "description": "eait.fit — the backend, the contract its clients implement, and the web application.",
  "workspaces": [
    "shared",
    "backend",
    "web"
  ],
  "scripts": {
    "start": "bun run --cwd backend start",
    "dev": "bun run --cwd backend dev",
    "demo": "bun run --cwd backend demo",
    "web": "bun run --cwd web start",
    "web:build": "bun run --cwd web build",
    "web:e2e": "playwright test",
    "typecheck": "bun run --cwd shared typecheck && bun run --cwd backend typecheck && bun run --cwd web typecheck && tsc -p scripts",
    "test": "bun test ./shared ./backend ./web",
    "check": "bun run typecheck && bun run web:build && bun run test && bun run openapi --check",
    "openapi": "bun run scripts/openapi.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/bun": "^1.4.1",
    "ts-json-schema-generator": "^2.9.0",
    "typescript": "^7.0.2"
  },
  "overrides": {
    "zod": "^4.5.4"
  }
}
